"use strict";

import Request from '../../index';

describe('request tests', () => {
	/**
	 *
	 */
	test.todo('TODO', () => {
		// expect.assertions(1);
		// let foo = Request();

		// expect(foo).toEqual(undefined);
	});
});
